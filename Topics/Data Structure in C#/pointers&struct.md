What Is a Pointer?

A pointer is a variable that stores the memory address of another variable.

A normal variable → stores a value (e.g., 10)

A pointer → stores the location where the value is stored
